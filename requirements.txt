# Dependencies for scripts/build_embeddings.py — the one Python script this
# repo still uses, to (re)generate web/data/embeddings.json for the web app.
google-genai>=1.52.0
pdfplumber>=0.11.4
python-dotenv>=1.2.1
